[package]
name = "validator"
version = "0.1.0"
edition = "2024"

[dependencies]
expectrl = "0.7"
tempfile = "3"
which = "4"
nix = { version = "0.27", optional = false, features = ["signal"] }

[dev-dependencies]
test-util = { path = "../test-util" }
